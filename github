$ git --version
$ touch index.html - create an html file
$git init
$ git config --global user.name 'Devin1996'
$ git config --global user.email 'devinchandula@gmail.com'
$ git add index.html
$ git status
$ git rm --cached index.html - remove index.html cached file
$ git add *.html - Add every files in html format
$ git add . - Add every files in every format

change  somthing in html and check status and put command 
now commit

$ git commit

click i to go for indsert mode

type initial mode

esc to exit from inser mode

exit -> : wq

after making changes to js file

$ git add . - Add every files in every format

$ git commit -m 'changed app.js'

$ clear

keep something u dnt want separeate

$ touch .gitignore

$ touch log.txt -> "Error logs"

.gitignore -> 'log.txt'

noe chech if log.txt is visible

$ git add .

$ git status

so it will be not available because we have log.txt to .gitignore file

create 2 new folders

dir1 -app1.js
dir2 -app2.js

.gitignore -> /dir2

$ git add .
$ git status

if you type *.txt in .gitignore file all .txt files will be unavailable

.........
branches
.........

$ git branch login
$ git status
$ git commit -m 'Another Change'

$ git checkout login -> swap to login 

$ touch login.html

index.html -> <p>login form</p>

$ git commit -m 'login form'


$ git checkout master -> swap to masrter

u can see that login and part we added to index html will be dissapear

$ git merge login -> merge master and login

i

give a commit with a reason "added login to the site"

:wq


.........
remote repository
.........

new repository 'myappsample'


Description  Sample app

create git hub repository

Now in bash

$ git remote -> nothing shows

paste this from git hub

git remote add origin https://github.com/Devin1996/myappsample.git

$ git remote rm origin

$git remote 

$git push -u origin master

#now it comes login window
#now reload the github page

$ touch README.md

				#app
				sample

$ git add .










